* Modify the response format in UI(create template format so that reponse  follows the structure)
* create a Human in loop structure to get user permission to execute them
* create a logging system to maintain the actions taken
* ~~Create a UI component to show the users about the data they are dealing with.~~
* The response should be explainable
